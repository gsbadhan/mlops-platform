name: mlops

services:

  backend:
    build:
      context: ./backend
    container_name: mlops-backend
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./frontend
    container_name: mlops-frontend
    ports:
      - "4200:80"
    depends_on:
      - backend